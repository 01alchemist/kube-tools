service-accounts: ${file(./service-accounts.yml)}
deployment: ${file(./deployment.yml)}
service: ${file(./service.yml)}
virtual-service: ${file(./virtual-service.yml)}
destination-rule: ${file(./destination-rule.yml)}
