apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: ${values:name}
spec:
  hosts:
  - ${values:name}
  http:
  - match:
    - uri:
        prefix: /
      queryParams:
        v:
          exact: ${values:subset}
    route:
    - destination:
        host: ${values:name}
        subset: ${values:subset}
