{
  "name": "@01/micro-services",
  "version": "1.0.0",
  "author": "Nidin Vinayakan <01@01alchemist.com>",
  "repository": "git@github.com:01alchemist/kube-tools.git",
  "private": true,
  "engines": {
    "node": ">=12"
  },
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "postinstall": "lerna bootstrap && yarn link @01/kube-tools && eval $(minikube docker-env)",
    "build": "eval $(minikube docker-env) && lerna run build --stream",
    "deploy": "lerna run deploy --stream"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "3.20.2"
  }
}
